const UserModel = require('./users.model');
const NoteModel = require('./notes.model');
const SharedNotesModel = require('./sharedNotes.model');

module.exports = {
  UserModel,
  NoteModel,
  SharedNotesModel,
};